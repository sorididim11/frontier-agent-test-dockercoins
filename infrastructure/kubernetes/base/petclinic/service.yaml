apiVersion: v1
kind: Service
metadata:
  name: petclinic
  namespace: petclinic
  labels:
    app: petclinic
    app.kubernetes.io/name: petclinic
    app.kubernetes.io/part-of: PetClinic-v2
spec:
  type: ClusterIP
  selector:
    app: petclinic
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
