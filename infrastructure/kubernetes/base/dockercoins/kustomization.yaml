apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

labels:
  - pairs:
      app.kubernetes.io/part-of: DockerCoins
    includeSelectors: false

resources:
  - namespace.yaml
  - redis.yaml
  - hasher.yaml
  - rng.yaml
  - worker.yaml
  - webui.yaml
  - instrumentation.yaml
  - cleanup-serviceaccount.yaml
  - cleanup-job.yaml
  - fis-serviceaccount.yaml
